#R Markdown
#----------
#Code will be showed but not executed
```{r eval=FALSE}

```
#Geoinformatics
#---------------
# check projection
projection()

#System
#-------
#Ordnet die Anzahl der Kerne des Rechners zu (i5 hat 4 cores, werden alle zugeordnet, kann nichts zusätzliches am Rechner gemacht werden)
cores() <- 3L

#save file
saveRDS(x, file = file.path(hp, "bsptif.rds")   ...hp: Speicherpfad

#read file
readRDS(x, 
readAll()

#load data into memory
r = readAll(r)


#Wenn Dateien so gespeichert werden, dass sie auf einen Link verweisen, ändert sich dieser nicht,
# wenn die Arbeitsumgebung geändert wird. Fehlerpotenzial.

#Für Arbeit auf Unirechner

# Umleiten des Systemlinks auf den Rechnern auf D:/ oder besser F:/ (externe Speichermedien)

hp = tolle_funktion(a="~/edu", b="D:/BEN/tnauss/")

#Ruft den Link zur Datei ab, aus der aktuellen Arbeitsumgebung (env), dort in der Datei "src", 
#   die Funktion "fun001"
file.path(env$path_src, "fun001")

#Um Funktionen wieder zu finden, Funktionalitäten mit Doppelpunkt aufrufen
link2GI::raster()

hp = alternativeEnvi(root_folder = "~/edu",
		     alt_envi_id = "COMPUTERNAME"

devtools::install.github